# Enable rewriting
RewriteEngine On

# Allow existing files/folders to be served
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Send everything else to index.php
RewriteRule ^ index.php [QSA,L]